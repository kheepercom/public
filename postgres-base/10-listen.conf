# Socket-only by default: no TCP listener. Layered images that expose Postgres
# on the network ship a higher-numbered snippet that overrides listen_addresses
# (e.g. the standalone postgres image's 30-listen.conf sets '*').
listen_addresses = ''
