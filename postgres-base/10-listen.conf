# Local-only by default: TCP on the loopback interface plus the unix socket.
# Layered images that want to expose Postgres on the network ship a
# higher-numbered snippet that overrides listen_addresses.
listen_addresses = 'localhost'
