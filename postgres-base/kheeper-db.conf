# Secure the rendered db-init SQL: it now carries app-user passwords, so it must
# not be world-readable. kheeper-db-init.service runs as postgres and reads it.
r /etc/kheeper/kheeper-db-init.sql
C /etc/kheeper/kheeper-db-init.sql 0640 root postgres - -
