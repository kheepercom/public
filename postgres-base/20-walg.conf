archive_mode = on
archive_command = 'set -a; . /etc/kheeper/walg.env; exec /usr/bin/timeout 30 /usr/local/bin/wal-g wal-push %p'
archive_timeout = 60
restore_command = 'set -a; . /etc/kheeper/walg.env; exec /usr/local/bin/wal-g wal-fetch %f %p'
