# Public Postgres: listen on every interface so the firewall-opened 5432/tcp
# is reachable. This snippet sorts after postgres-base's 10-listen.conf, so
# its listen_addresses wins.
listen_addresses = '*'
